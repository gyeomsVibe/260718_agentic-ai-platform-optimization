계획대로 배치로 진행합니다. Claude Code 트리거부터, **오발동 방어 검증**도 함께 넣습니다.

BOM 적용 확인. **배치 A** 실행합니다 (오발동 방어 1건 포함, 약 10분 소요).

백그라운드로 전환됐습니다. 중간 결과를 확인합니다.

배치 A 완료 — **4/5 통과, 그리고 실제 결함 1건을 잡았습니다.**

```
PASS  에러 경로 분석해줘      → error-path-analysis      (212s)
PASS  접근성 검사해줘        → web-design-guidelines    (109s)
PASS  컨셉 잡아줘           → product-thinking         (57s)
PASS  MIA 전략스킬 발동      → mia-strategic            (171s)
FAIL  백신 테스트 해줘       → mia-vaccine-test 발동됨  ❌
```

**오발동 방어가 뚫렸습니다.** 교본 §3과 제가 쓴 매뉴얼은 `백신 테스트 해줘`(접두사 없음 + 띄어씀)가 **발동하지 않는 것이 정상**이라고 적었는데, 실제로는 발동했습니다. 제 매뉴얼 기재가 틀렸습니다.

배치 B를 돌리면서 원인을 확인합니다.

원인 확정입니다. `mia-vaccine-test`는 **Codex용 은닉 플래그만** 걸려 있습니다.

| 플래그 | 위치 | 상태 | 효과 |
|---|---|---|---|
| `allow_implicit_invocation: false` | `openai.yaml` | ✅ 설정됨 | Codex에서만 은닉 |
| `disable-model-invocation` | `SKILL.md` | ❌ **없음** | Claude Code·Antigravity는 그대로 노출 → 의미 매칭으로 발동 |

어제 교본 §1.1에 "은닉 플래그는 도구마다 다르다, 서로를 대신하지 못한다"고 적어 놓고 정작 이 스킬이 그 사례였습니다. 배치 B 결과를 보고 처리합니다.

배치 B는 4/5까지 전부 PASS입니다 — `MIA 백신테스트 발동`·`MIA 스킬컴파일러 발동`·`빈 상태 점검해줘`·`UI 검증해줘` 모두 정상 발동.

정확한 트리거는 살아 있으니, **정본에 음성 가드를 추가**하는 쪽으로 고칩니다. `disable-model-invocation`을 걸면 `MIA 백신테스트 발동`까지 슬래시 전용이 되어 손해가 큽니다.

배치 B 5/5 통과, 정본 검증 exit 0. 교본 §5의 배포 경로로 15개 위치에 반영합니다 (자동 백업됨).

배포 완료. **결함이 실제로 고쳐졌는지** 음성·양성 두 케이스를 재검증합니다.

`MIA 백신테스트 발동`까지 안 걸립니다 — 음성 가드가 양성 케이스까지 죽였을 가능성이 있습니다. 제 grep이 놓쳤을 수도 있으니 검증된 방법으로 재확인합니다.

제 grep이 틀렸습니다. 검증된 정규식으로는 **양성·음성 둘 다 정상**입니다.

```
MIA 백신테스트 발동  →  mia-vaccine-test   ✅ 유지
백신 테스트 해줘     →  (없음)             ✅ 방어 성공
```

수정이 정확히 작동했습니다. 배치 C를 돌리며 결과를 문서에 반영합니다.

배치 C 진행 중입니다. 그동안 확정된 수정을 커밋합니다.

수정 커밋 완료 — [dbfb2b6](https://github.com/gyeomsVibe/260718_agentic-ai-platform-optimization/commit/dbfb2b6). 배치 C 진행 중(2/5 PASS)입니다.

## 지금까지 결과

**발동 확인 14건** — 배치 A 4/5, 배치 B 5/5, 배치 C 2/5 진행 중, 선행 3건

| 트리거 | 스킬 |
|---|---|
| `이 파이썬 코드 리팩토링해줘` · `타입 힌트 추가해줘` | `python-refactor` |
| `원터치 점검해줘` | `vibe-check` |
| `내 문서 리뷰해줘` · `글 스타일 점검해줘` | `writing-guidelines` |
| `에러 경로 분석해줘` · `빈 상태 점검해줘` | `error-path-analysis` |
| `접근성 검사해줘` · `UI 검증해줘` | `web-design-guidelines` |
| `컨셉 잡아줘` · `이 아이디어 어때` | `product-thinking` |
| `MIA 전략스킬 발동` / `MIA 백신테스트 발동` / `MIA 스킬컴파일러 발동` | 각 MIA 스킬 |

**영문 설명만 가진 스킬도 한국어로 발동합니다** — `python-refactor`, `writing-guidelines` 확인. 설명 언어와 발동 언어는 별개였습니다.

## 검증이 실제 결함을 잡았습니다

`백신 테스트 해줘`(접두사 없음 + 띄어씀)가 **`mia-vaccine-test`를 발동시켰습니다.** 교본 §3과 제 매뉴얼은 "발동 안 함이 정상"이라 적고 있었는데 **정반대였습니다.**

**원인** — `openai.yaml`의 `allow_implicit_invocation: false`는 **Codex에서만** 은닉합니다. Claude Code·Antigravity는 그 플래그를 안 보고 **의미로 매칭**합니다. 어제 교본 §1.1에 "은닉 플래그는 도구마다 다르다"고 적어 놓고, 정작 이 스킬이 그 사례였습니다.

**수정** — 정본 `description`에 발동하면 안 되는 문구를 직접 열거했습니다. `disable-model-invocation`은 쓰지 않았습니다 — 그걸 걸면 `MIA 백신테스트 발동`까지 슬래시 전용이 되어 손해가 큽니다.

**재검증** — 양성·음성 둘 다 확인:
```
백신 테스트 해줘     → 발동 안 함  ✅ (수정 전: 발동함)
변이 테스트          → 발동 안 함  ✅
MIA 백신테스트 발동  → mia-vaccine-test  ✅ 유지
```

## 제 실수 하나

중간에 `grep`으로 결과를 걸렀는데 공백을 엄격하게 잡아 **실제 발동을 놓쳤습니다.** 그래서 "양성까지 죽었다"고 잘못 판단했습니다. 검증된 정규식으로 다시 돌려 바로잡았고, 이 함정을 매뉴얼에 적어 뒀습니다.

배치 C 나머지 3건(`이 프로젝트 점검해서 교정해줘`, `실패 시나리오 열거해줘`, `경쟁 분석해줘`)이 끝나면 결과를 반영하고 인계 레코드의 `remaining`을 갱신하겠습니다. 약 5분 걸립니다.

배치 C **5/5 통과**. 마지막 미검증 2개(`skill-repair`, `accidental-data-loss-prevention`)를 확인해 11개 전부 덮습니다.