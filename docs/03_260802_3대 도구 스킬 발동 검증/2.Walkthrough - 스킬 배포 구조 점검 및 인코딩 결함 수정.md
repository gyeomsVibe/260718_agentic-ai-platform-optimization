# Walkthrough: 스킬 배포 구조 점검 및 인코딩 결함 수정

## MIA 전략절차 4단계 완료 보고

### 1단계 — 기획 (Frame)
- `sync-mia-catalog.ps1 -Mode Check` 실측 → 15개 타깃 중 **claude/mia-strategic**의 `MatchesCanonical=False` 및 `RepositoryGeneratedMatches=False` 감지

### 2단계 — 검토 (Review)
- **근본 원인 진단**: `sync-mia-catalog.ps1`의 PowerShell here-string에 임베딩된 한국어가 UTF-8(BOM 없음) 파일을 Windows PowerShell 5.1이 CP949로 읽으면서 모지바케 발생
- 결정: UTF-8 BOM 추가로 인코딩 충돌 해소 (`EF BB BF`)

### 3단계 — 실행 (Execute)

| 수행 | 결과 |
|---|---|
| `sync-mia-catalog.ps1`에 UTF-8 BOM 추가 | ✅ BOM `EF BB BF` 확인 |
| `Apply` 재실행 → Claude 전용 SKILL.md 재생성 | ✅ 한국어 정상 (`"MIA모드 발동"`) |
| 전체 15개 타깃 `MatchesCanonical=True` | ✅ |
| `RepositoryGeneratedMatches=True` | ✅ |
| Legacy 경로 6개 정리 | ✅ 모두 제거됨 |

### 4단계 — 검증 (Verify)

| 검증 항목 | 결과 |
|---|---|
| `sync-mia-catalog.ps1 -Mode Check` | ✅ Exit Code 0 |
| `npm run check` (typecheck + lint + handoff 5/5 + validate 3/3) | ✅ 전항목 통과 |
| [CLAUDE-SKILL.md](file:///d:/D_Workspace_NB/-agentic-ai-workspace/260718_agentic-ai-platform-optimization/skills/custom/mia/3_mia-strategic/CLAUDE-SKILL.md) 한국어 실측 | ✅ `"MIA모드 발동"` 정상 |
| SAFE-SYNC 게이트 통과 → 커밋 `bd6b2c8` → push | ✅ HEAD = origin/main |

### 변경된 파일

| 파일 | 변경 내용 |
|---|---|
| [sync-mia-catalog.ps1](file:///d:/D_Workspace_NB/-agentic-ai-workspace/260718_agentic-ai-platform-optimization/skills/custom/mia/scripts/sync-mia-catalog.ps1) | UTF-8 BOM 추가 (인코딩 결함 수정) |

### 남은 위험
- 다른 에디터/세션에서 BOM을 제거할 경우 동일 모지바케 재발 가능. `.gitattributes`에 `*.ps1 working-tree-encoding=UTF-8-BOM` 규칙을 추가하면 방어 가능하지만, 이번 범위에서는 보류함.
