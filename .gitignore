# 로컬 전용 에이전트 설정 (권한 등)
.claude/settings.local.json

# OS / 편집기 잡파일
Thumbs.db
.DS_Store
*.log
shared/platform-auto-update/scripts/npm-cache/

# 로컬 검증·임시 Skill 설치 영역
node_modules/
.agents/
.analysis-tmp/
*.tsbuildinfo
coverage/

# 비밀 정보 (안전장치)
.env
*.pem
*.key

# 개인 사용자 데이터(영상·자료 등) — 커밋 금지
claude/[[]user data[]]/
