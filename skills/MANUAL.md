# Skill 사용 매뉴얼

> **이 문서는 "스킬이 뭔지 모르는 사람"을 위해 씁니다.** 용어부터 설명하고, 각 스킬을
> **어떤 문장으로 부르면 되는지** 그대로 적어 뒀습니다. 복사해서 붙여 넣어도 됩니다.
>
> 기준 시점: **2026-08-05** · 3대 도구 CLI로 스킬 설치·인식을 확인한 상태입니다.
> 배포 선별 근거는 [DEPLOYMENT.md](DEPLOYMENT.md), 제작 규격은
> [custom/mia/AUTHORING_HANDBOOK.md](custom/mia/AUTHORING_HANDBOOK.md)를 보세요.

### 트리거 문구 검증 상태 (2026-08-05)

아래 문구는 실제로 헤드리스 세션에 입력해 **Skill 도구가 호출된 로그로** 확인했습니다.
모델의 자기보고가 아니라 실행 증거입니다.

**발동 확인 (12건)**

| 트리거 | 발동한 스킬 |
|---|---|
| `이 파이썬 코드 리팩토링해줘` · `타입 힌트 추가해줘` | `python-refactor` |
| `원터치 점검해줘` · `이 프로젝트 점검해서 교정해줘` | `vibe-check` |
| `내 문서 리뷰해줘` · `글 스타일 점검해줘` | `writing-guidelines` |
| `에러 경로 분석해줘` · `빈 상태 점검해줘` · `실패 시나리오 열거해줘` | `error-path-analysis` |
| `접근성 검사해줘` · `UI 검증해줘` | `web-design-guidelines` |
| `컨셉 잡아줘` · `이 아이디어 어때` · `경쟁 분석해줘` | `product-thinking` |
| `MIA 전략스킬 발동` | `mia-strategic` |
| `MIA 백신테스트 발동` | `mia-vaccine-test` |
| `MIA 스킬컴파일러 발동` | `mia-skill-compiler` |

> 영문 설명만 가진 스킬(`python-refactor`, `writing-guidelines`)도 **한국어 문구로 발동**함을
> 확인했습니다. 설명 언어와 발동 언어는 별개입니다.

**오발동 방어 확인 (2건)**

| 잘못된 문구 | 결과 |
|---|---|
| `백신 테스트 해줘` (접두사 없음 + 띄어씀) | 발동 안 함 ✅ |
| `변이 테스트` | 발동 안 함 ✅ |

> ⚠️ **이 방어는 2026-08-05에 뚫려 있던 것을 고친 결과입니다.** `mia-vaccine-test` 는
> Codex용 은닉 플래그(`allow_implicit_invocation: false`)만 걸려 있어 Claude Code 에서는
> `백신 테스트 해줘` 에도 발동했습니다. 정본 `description` 에 음성 가드를 추가해 해소했고,
> 양성(`MIA 백신테스트 발동`)이 유지되는 것까지 재확인했습니다.

**아직 검증하지 않은 문구가 남아 있습니다.** 스킬 설명에서 뽑은 것이라 대체로 맞겠지만
보증하지는 않습니다. 안 걸리면 6장 ②처럼 **이름을 직접 부르세요.**

검증을 이어서 하려면 이 명령 형식을 쓰면 됩니다.

```bash
claude -p "<트리거 문구>" --permission-mode plan --output-format stream-json --verbose
```

출력에서 `"name":"Skill","input":{"skill":"..."}` 를 찾으면 그 스킬이 발동한 것입니다.
`grep` 으로 거를 때 공백을 엄격하게 잡으면 실제 발동을 놓칠 수 있으니 주의하세요.

---

## 1. 5분 개념 — 이것부터

### 스킬(Skill)이 뭔가요

**AI에게 미리 가르쳐 둔 작업 설명서**입니다.

비유하자면 요리사에게 건네는 레시피 카드입니다. "김치찌개 해줘"라고만 하면 요리사가
자기 방식대로 만들지만, 레시피 카드를 미리 줘 두면 **매번 같은 순서와 기준으로** 만듭니다.

스킬이 없으면 AI는 그때그때 다르게 일합니다. 스킬이 있으면 정해진 절차·점검 항목·출력
형식을 따릅니다. **결과의 편차가 줄어드는 것**이 핵심 이득입니다.

### 어떻게 작동하나요

AI는 대화 시작 시 **설치된 스킬들의 "이름 + 한 줄 설명"만** 미리 읽어 둡니다.
그러다 당신의 요청이 어떤 설명과 맞아떨어지면, 그때 **그 스킬의 본문 전체를 펼쳐 읽고**
그 절차대로 일합니다.

그래서 두 가지가 따라옵니다.

- **스킬이 많을수록 손해도 생깁니다.** 설명 목록이 길어져 엉뚱한 스킬이 튀어나옵니다.
  그래서 이 워크스페이스는 **쓸 것만 골라서** 설치합니다 ([DEPLOYMENT.md](DEPLOYMENT.md)).
- **말하는 방식이 중요합니다.** 설명에 적힌 표현과 비슷하게 말해야 걸립니다.
  아래 매뉴얼에 **실제 발동 문구**를 그대로 적어 둔 이유입니다.

### 서브에이전트(Subagent)는 또 뭔가요

**스킬**은 지금 대화하는 AI가 읽는 설명서입니다.
**서브에이전트**는 아예 **다른 AI 한 명을 따로 불러서** 일을 통째로 맡기는 것입니다.

| | 스킬 | 서브에이전트 |
|---|---|---|
| 누가 일하나 | 지금 대화 중인 AI | 새로 뜬 별도 AI |
| 대화 기록 | 이어짐 | **처음부터 시작(백지)** |
| 좋은 점 | 맥락 유지, 빠름 | 긴 작업을 격리, 병렬 가능 |
| 나쁜 점 | 긴 작업 시 대화가 길어짐 | 지금까지 얘기한 걸 모름 |

서브에이전트는 **Claude Code에만** 있습니다. 보통은 AI가 알아서 판단해 부르므로
당신이 이름을 외울 필요는 없습니다.

### 왜 도구마다 스킬이 다른가요

세 도구가 **서로 다른 폴더**를 읽기 때문입니다. 공용 폴더는 **없습니다.**

| 도구 | 읽는 폴더 |
|---|---|
| Claude Code | `~/.claude/skills` |
| Codex | `~/.codex/skills` + `~/.agents/skills` |
| Antigravity | `~/.gemini/config/skills` 계열 |

> `~`는 `C:\Users\Kimyoongyeom` 을 뜻합니다.

한 도구에 깔았다고 다른 도구에서 되지 않습니다. **한 도구에서 됐다고 다른 도구도 될 거라
넘겨짚지 마세요.** 실제로 그렇게 오래 잘못 알고 있었습니다.

---

## 2. 지금 내 PC 상태 (2026-08-05 실측)

| 도구 | 스킬 | 서브에이전트 |
|---|---|---|
| **Claude Code** | 11개 | 4개 |
| **Codex** | 44개 (가장 많음) | — |
| **Antigravity** | 19개 (+ 과학 플러그인 39개) | — |

**작업 성격에 따라 도구를 고르세요.** 문서·기획·스킬 정비는 Claude Code, 코드 작업 폭이
넓으면 Codex, 과학·데이터베이스 조회는 Antigravity가 유리합니다.

---

## 3. Claude Code 매뉴얼

### 3-1. 스킬 11개

#### 🔧 프로젝트 점검·수리

**`vibe-check` — 원터치 자가진단**
프로젝트를 스스로 진단하고, 실패한 것을 최소한으로 고치고, 다시 진단해서 보고까지 합니다.
당신은 큰 승인만 하면 됩니다.

```
원터치 점검해줘
이 프로젝트 점검해서 교정해줘
vibe-check 해줘
진단 돌리고 실패한 것 고쳐줘
```

> 윤겸스의 전역 규칙이 지정한 공식 점검 경로입니다. **뭘 해야 할지 모를 때 이걸 먼저 부르세요.**

**`skill-repair` — 스킬이 고장났을 때**
설치가 깨진 스킬을 고쳐서 다시 설치합니다.

```
스킬이 안 뜨는데 고쳐줘
skill-repair
```

#### ✍️ 문서·글

**`writing-guidelines` — 문서 문체 점검**
써 둔 문서가 글쓰기 기준에 맞는지 검토합니다. 표현, 어조, 일관성을 봅니다.

```
내 문서 리뷰해줘
글 스타일 점검해줘
review my docs
check writing style
```

#### 🎨 UI·화면

**`web-design-guidelines` — 접근성·UX 100+ 규칙 감사**
완성된 UI 코드를 100개 넘는 규칙으로 검사하고 `파일:줄번호` 형식으로 위반 목록을 냅니다.

```
UI 검증해줘
접근성 검사해줘
코드 감사해줘
check accessibility
```

**`error-path-analysis` — 에러 났을 때 사용자 경험 진단**
"잘 될 때"가 아니라 **"안 될 때"** 사용자가 뭘 보는지 점검합니다. 빈 화면, 에러 메시지,
오프라인 상황을 봅니다.

```
에러 경로 분석해줘
에러 핸들링 점검해줘
실패 시나리오 열거해줘
빈 상태 점검해줘
오프라인 대응 확인해줘
```

#### 🐍 코드

**`python-refactor` — 파이썬 코드 정리**
함수 정리, 타입 힌트·docstring 추가, 중복 제거, 테스트·린트 준비를 합니다.

```
이 파이썬 코드 리팩토링해줘
타입 힌트 추가해줘
docstring 붙여줘
```

#### 💡 기획

**`product-thinking` — 뭘 만들지 정하기**
기능이 아니라 **컨셉**을 다룹니다. 핵심 질문 평가, 관점 전환, 경쟁자 3층 분석,
가치 제안 도출.

```
컨셉 잡아줘
기획 방향 점검해줘
이 아이디어 어때
경쟁 분석해줘
무엇을 만들지 모르겠어
```

> 만들 것이 이미 정해졌다면 이 스킬이 아닙니다. 바로 구현으로 가세요.

#### 🛡️ 안전

**`accidental-data-loss-prevention` — 실수로 지우는 것 방지**
되돌릴 수 없는 삭제(DB 테이블 삭제, 클라우드 파일 삭제 등) 전에 **반드시 당신 확인을 받게**
합니다. 당신이 부르는 게 아니라 **AI가 위험한 순간에 스스로 발동**합니다.

#### 🧭 MIA 시리즈 (윤겸스 전용 제작 스킬)

이 셋은 **접두사 `MIA` + 붙여쓴 모드 이름 + `발동`/`해줘`** 형식을 지켜야 합니다.
띄어 쓰거나 접두사를 빼면 **일부러 발동하지 않습니다.** 오발동을 막기 위한 설계입니다.

| 스킬 | 무엇을 하나 | 발동 문구 |
|---|---|---|
| `mia-strategic` | 전략적 가설 검증 — 기획→검토→실행→검증 4단계 | `MIA모드 발동` · `MIA 전략스킬 발동` · `MIA 전략절차 해줘` |
| `mia-skill-compiler` | 새 스킬을 만들거나 기존 스킬 개선 | `MIA 스킬컴파일러 발동` · `MIA 스킬컴파일러 해줘` |
| `mia-vaccine-test` | 일부러 고장 내서 숨은 결함 찾기 + 회귀 테스트 남기기 | `MIA 백신테스트 발동` · `MIA 변이테스트해줘` · `MIA 숨은결함 찾아줘` |

```
❌ 백신 테스트 해줘      (접두사 없음, 띄어씀 → 발동 안 함이 정상)
✅ MIA 백신테스트 발동
```

### 3-2. 서브에이전트 4개

**보통은 AI가 알아서 부릅니다.** 아래는 "이런 요청을 하면 저 전문가가 붙는다"는 참고입니다.

| 에이전트 | 언제 붙나 | 이렇게 말하면 |
|---|---|---|
| `api-architect` | API·백엔드 설계 | `API 설계해줘` · `엔드포인트 추가해줘` · `인증 흐름 짜줘` |
| `frontend-design-specialist` | UI 만들기·접근성 | `UI 만들어줘` · `랜딩 페이지 만들어줘` · `반응형으로 해줘` |
| `test-writer` | 테스트 작성 | `테스트 작성해줘` · `TDD로 구현해줘` · `이 함수 테스트해줘` |
| `technical-writer` | 문서 작성 | `README 작성해줘` · `API 문서화해줘` · `체인지로그 만들어줘` |

> **주의**: 이 PC에는 Playwright(E2E 테스트 도구)가 설치돼 있지 않습니다.
> `test-writer`는 E2E를 요청받으면 **먼저 그 사실을 보고**하고 멈춥니다. 정상 동작입니다.

---

## 4. Antigravity 매뉴얼

Claude Code와 **겹치는 9개**(`vibe-check`, `writing-guidelines`, `python-refactor`,
`skill-repair`, `accidental-data-loss-prevention`, `error-path-analysis`,
`web-design-guidelines`, `product-thinking`, `mia-*`)는 3장과 발동 문구가 같습니다.

### Antigravity에만 있는 것

| 스킬 | 무엇을 하나 | 이렇게 말하면 |
|---|---|---|
| `security-audit` | 보안 취약점·API 키 유출 검사 | `보안 검토해줘` · `취약점 점검해줘` |
| `git-commit-helper` | Conventional Commits 형식 커밋 메시지 작성 | `커밋 메시지 써줘` |
| `api-review` | API 엔드포인트·스키마·인증 검토 | `API 검토해줘` · `엔드포인트 점검해줘` |
| `frontend-design` | 템플릿 같지 않은 개성 있는 시각 디자인 방향 | `디자인 방향 잡아줘` · `UI 다시 디자인해줘` |
| `frontend-polish` | UI 완성도·반응형·빈 상태·로딩 상태 다듬기 | `UI 다듬어줘` · `반응형으로 만들어줘` |
| `vercel-react-best-practices` | Vercel 엔지니어링의 React·Next.js 성능 기준 | `React 성능 최적화해줘` · `Next.js 리뷰해줘` |
| `managing-python-dependencies` | 전역 `pip install` 방지, 프로젝트별 의존성 관리 | `pip install` 시도 시 **자동 발동** |
| `grilling` | 계획·결정을 질문으로 집요하게 압박해 검증 | `내 계획 압박 질문해줘` · `grill me` |
| `grill-me` | 위와 같으나 **목록에 안 뜸** — 이름을 직접 불러야 함 | `$grill-me` |

**추가로 과학 플러그인 39개**가 붙어 있습니다 — 단백질 구조(AlphaFold), 유전체(gnomAD,
Ensembl), 논문 검색(PubMed, arXiv), 화합물(PubChem) 등. 생명과학 데이터가 필요할 때만
쓰이고, 평소에는 방해되지 않습니다.

---

## 5. Codex 매뉴얼

**스킬이 가장 많은 도구(44개)입니다.** Claude Code·Antigravity에 없는 것이 여기 다 있습니다.

### Codex에만 있는 스킬 — 분야별

#### 코드 품질
| 스킬 | 발동 문구 |
|---|---|
| `codebase-analyzer` — 코드 리뷰·버그 탐지·리팩토링·dead code | `코드 리뷰해줘` · `버그 찾아줘` · `전체 점검해줘` · `미사용 코드 찾아줘` |
| `dependency-manager` — 오래된 패키지·미사용 의존성 점검 | `의존성 점검` · `패키지 업데이트` · `outdated 확인` |
| `commit` — 검증 후 안전한 커밋 | `커밋` · `commit` |
| `retro` — 세션 노트 + git 이력으로 회고 생성 | `회고` · `retro` |

#### 배포·운영
| 스킬 | 발동 문구 |
|---|---|
| `deploy-safety-guard` — 배포 전 5단계 안전 점검 | `배포 안전 점검` · `운영 체크` · `환경변수 검증` |
| `performance-checker` — 빌드·번들·성능 | `성능 점검` · `빌드 체크` · `배포 전 점검` |
| `site-auditor` — 성능+디자인+SEO+백엔드 통합 감사 | `사이트 감사해줘` · `종합 점검` |
| `seo-master` — Next.js SEO·메타태그 | `SEO 점검` · `메타태그 확인` |

#### 기획·문서
| 스킬 | 발동 문구 |
|---|---|
| `start-docs` — PRD·TRD·ERD 일괄 생성 | `프로젝트 시작` · `기획 문서` · `문서 생성` |
| `idea` — 아이디어 검증 | `아이디어 검증` |
| `debate` — 여러 전문가 관점 토론으로 의사결정 | `토론해줘` · `다각도 분석` · `장단점 비교` |
| `ebook-writing` — 전자책 챕터 집필·검토 | `전자책 써줘` · `챕터 작성` · `원고 작성` |

#### 웹·자동화
| 스킬 | 발동 문구 |
|---|---|
| `agent-browser` — 헤드리스 크롤링·스크래핑 | `크롤링해줘` · `스크래핑` |
| `webapp-testing` — 로컬 웹앱 화면·로그 검증 | `웹앱 테스트해줘` |
| `b2b-landing` — B2B SaaS 랜딩 페이지 생성 | `B2B 랜딩` · `SaaS 랜딩` |
| `remotion-studio` — 이미지·코드·데이터를 영상으로 | `영상 만들어줘` · `remotion 영상` |
| `orchestrate` — 여러 작업을 에이전트에 병렬 위임 | `오케스트레이션` · `작업 분배` · `병렬 위임` |

#### 법무
| 스킬 | 발동 문구 |
|---|---|
| `korean-privacy-terms` — 한국 법령 기반 개인정보처리방침 | `개인정보처리방침 만들어줘` |

> ⚠️ **주의**: 이 스킬의 **한국어 이용약관 템플릿이 비어 있습니다**(원본 제작자 쪽 결함).
> 기본 설정으로 약관을 만들면 **빈 문서가 나옵니다.** 그래서 0바이트를 감지하면 멈추고
> 알려 주도록 안전장치를 넣어 뒀습니다. 처리방침은 정상이고, 약관은 영문판을 쓰거나
> 법률 검토를 거친 원문을 따로 구해야 합니다.

#### 이 저장소와 충돌하는 것 — 쓰지 마세요
`sync-claude-md`, `sync-workflow`는 `CLAUDE.md`를 자기 규칙대로 고칩니다.
**이 저장소의 SAFE-SYNC 게이트·handoff 체계와 충돌**하므로 Claude Code에는 일부러
설치하지 않았습니다. Codex에서도 이 저장소 안에서는 쓰지 마세요.

---

## 6. 안 될 때 — 순서대로 확인

### ① 새 대화를 여세요

**스킬 목록은 대화가 시작될 때 한 번 읽힙니다.** 방금 설치했다면 지금 대화에서는
안 보일 수 있습니다. 새 대화·새 세션을 열면 반영됩니다.

### ② 이름을 직접 부르세요

돌려 말하면 안 걸릴 수 있습니다. **스킬 이름을 그대로 말하는 것이 가장 확실합니다.**

```
web-design-guidelines 스킬 써서 이 화면 점검해줘
```

### ③ 도구를 확인하세요

Codex에 있는 스킬을 Claude Code에서 부르고 있는지 확인하세요. 2장의 표를 보세요.

### ④ 그래도 안 되면 — 직접 확인하는 명령

터미널에 붙여 넣으면 그 도구가 실제로 뭘 보고 있는지 나옵니다.

```bash
claude -p "사용 가능한 스킬 이름을 쉼표로 나열만 하세요." --permission-mode plan
```

```bash
codex exec "사용 가능한 스킬 이름을 쉼표로 구분해 전부 나열만 하라."
```

목록이 길면 AI가 몇 개를 빠뜨릴 수 있습니다. 특정 스킬만 확인하려면 **이름을 지목**하세요.

```bash
claude -p "vibe-check 스킬이 목록에 있는지 있음/없음으로만 답하세요." --permission-mode plan
```

### ⑤ 스킬 파일 자체가 깨졌는지 검사

저장소 루트에서 실행하면 4개 도구 폴더 전체를 규격 검사합니다.

```bash
npm run skills:audit
```

`오류 0건`이 정상입니다. 오류가 나오면 어느 폴더의 어느 스킬이 왜 깨졌는지 알려 줍니다.

---

## 7. 자주 하는 오해

| 오해 | 사실 |
|---|---|
| "파일 복사했으니 이제 되겠지" | **아닙니다.** 복사는 설치일 뿐, 도구가 인식하는지는 별개입니다. 6-④로 확인하세요 |
| "Codex에서 되니까 Claude에서도 되겠지" | **아닙니다.** 폴더가 다릅니다. `~/.agents/skills` 40개는 Claude Code에서 **0개** 보입니다 |
| "스킬은 많을수록 좋다" | **아닙니다.** 목록이 길수록 엉뚱한 스킬이 발동합니다. 그래서 40개 중 골라서 깔았습니다 |
| "Codex 목록에 없으면 고장" | **아닐 수 있습니다.** 일부러 숨긴 것이 있습니다(`allow_implicit_invocation: false`). 이름을 직접 부르면 동작합니다 |
| "지금 대화에서 안 보이면 실패" | **아닙니다.** 새 대화부터 반영됩니다 |

---

## 8. 관련 문서

| 문서 | 언제 보나 |
|---|---|
| [DEPLOYMENT.md](DEPLOYMENT.md) | 어느 스킬을 어느 도구에 왜 올렸는지 |
| [custom/mia/AUTHORING_HANDBOOK.md](custom/mia/AUTHORING_HANDBOOK.md) | 새 스킬을 만들 때의 규격·검증 절차 |
| [external/](external/) | 외부에서 가져온 스킬의 출처·라이선스 |
| [README.md](README.md) | 이 폴더의 분류 원칙 |
