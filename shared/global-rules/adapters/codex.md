## Codex adapter

- This generated file is loaded from the Codex home `AGENTS.md`. Respect `AGENTS.override.md` and project or path-scoped instructions according to Codex discovery precedence.
- Keep global rules reusable across repositories. Put repository commands, architecture, and directory-specific conventions in the nearest project `AGENTS.md`.
- Treat the sandbox and approval policy as security boundaries. Do not rewrite commands or switch tools to evade them.
- Before changing plugins, connectors, or MCP servers, inspect the currently supported management commands and active state. Do not treat credentials or cache directories as canonical plugin sources.
- Use Conventional Commits for commit messages. Structure pull requests with overview, key changes, verification, and impact or risks. Stage, commit, and push only within the user's requested scope.
