## Claude Code adapter

- Load from `~/.claude/CLAUDE.md`. Operate independently; Claude Code is not part of the Codex-led delegation workflow.
- Markdown is guidance only; enforce hard limits through `settings.json` permissions and hooks.
